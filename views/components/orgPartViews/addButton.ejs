<div class="d-flex justify-content-end">
    <div class="d-flex align-items-center p-1">
        <div>
            <% if (type=="Cluster") {%>
                <button type="button" class="btn btn-primary" onclick="displayExportMessage(event, '<%=groupName%>', '<%=groupId%>', 'All')">
            <% } else if (type=="Sub-Projects") { %>
                <button type="button" class="btn btn-primary" onclick="displayExportMessage(event, '<%=groupName%>', '<%=groupId%>', 'Cluster')">
            <% } else { %>
                <button type="button" class="btn btn-primary" onclick="displayExportMessage(event, '<%=groupName%>', '<%=groupId%>', 'SHG')">
            <% } %>
                <i class='bx bx-export '></i>
                <span class="d-none d-sm-inline">Export</span>
            </button>
        </div>
    </div>
    
    <div class="d-flex align-items-center">
        <div>
            <button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#add<%=type%>">
                <i class='bx bx-plus'></i>
                <span class="d-none d-sm-inline">Add</span>
            </button>
        </div>
    </div>
</div>