
<table id="membersTable" class="table table-striped table-hover table-bordered nowrap" style="width:100%">
    <thead>
        <tr>
            <!-- Adjust colspan to fit the number of cols of data needed -->
            <th colspan = "3" class = "text-center">YEAR *update based on choice*</th>
            <% const months = ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'June', 'July', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec']%>
            <% for(let i = 0; i < months.length; i++) { %>
                <th colspan = "2" class = "text-center"><%=months[i]%></th>
            <% } %>
            <th>Year</th>
        </tr>
        <tr>
            <th>Last Name</th>
            <th>First Name</th>
            <th>ID</th>
            <% for(let i = 0; i < months.length; i++) { %>
                <th>Savings</th>
                <th>Grant</th>
            <% } %>
            <th>Total Savings</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <% var month = ['jan', 'feb', 'mar', 'apr', 'may', 'jun', 'jul', 'aug', 'sep', 'oct', 'nov', 'dec']; %>
            <% for (let i = 0; i < pageParts.length; i++) { %>
                <td><%=pageParts[i].name.lastname%></td>
                <td><%=pageParts[i].name.firstname%></td>
                <td><%=pageParts[i].id%></td>
                    <% for(var j=0; j<months.length; j++) { %>
                        <td><%= memberSavings[i][month[j]].savings %></td>
                        <td><%= memberSavings[i][month[j]].match %></td>
                    <% } %>
                <td><%= memberSavings[i].totalSavings %></td>
            <% } %>
        </tr>
    </tbody>
</table>
